# V1.2 Integrity Repair Pass — worklog (resume point)

## BINDING AMENDMENTS (founder, m0014) — override earlier plan where they conflict
1. UMD core module at ROOT `incommon-core.js` — prototype and tests execute the SAME file, no transform/strip. Node consumption via module.exports branch; browser via window.InCommonCore. Prototype loads it with helmet `<script src="./incommon-core.js">` + poll-ready in componentDidMount.
2. Split classification: numerology ENGINE (PASS eligible after arbitrary-input executed tests) vs user-facing FLOW (DEMO until onboarding is source of truth → V1.2 makes profile the source of truth: numerology computes live from profile DOB/name; astrology placements stay Maya demo w/ banner; timeMode gates Rising/houses/HD/Sabian across Natal/Today/HD/Stella context).
3. Integration tests REQUIRED (real component, iframe): (a) B1 delete→debounce→reload→remount→storage empty; (b) exact/approx/unknown across screens; (c) consent revoke removes Jordan data + filters Stella history; (d) prototype provably calls core (reference identity check via hook + numerology values === core output). Enablers in V1.2 fork: `?test=1` → storage keys suffixed `_test` (never touch user's real keys in tests); `window.__incommon = this` dev hook in componentDidMount (documented).
4. test-results-v1.2.json generated FROM execution, never composed: Test Runner DC auto-runs, computes SHA-256 of core+tests source (crypto.subtle over fetched text), logs chunked `__TESTJSON__[i/n]` lines; I show_html it, get_webview_logs, assemble verbatim, write file. JSON fields: timestamp, environment (UA), sourceHashes, assertions[{id,name,pass,expected,actual}], totals, exitCode (0/1). Also ship run-tests-node.js (pure tests in Node, DOM ITs reported "skipped: browser-only").
5. Citation validation resolves chips → real enabled source IDs via registry (Natal/Numerology→chart, Transit/Moon→transits, Insight·date→insights + date must exist in records, Journal→journal, Assessment→assess, Synastry/Two-person→duo, Scripted/Safety→always). ≥1 chip required; missing SOURCES line or unknown/disabled id → reply withheld + visible failure notice. Never trust model-named sources.
6. Separate informed model-processing consent modal BEFORE first live bridge call: names provider ("the embedding host's Claude bridge — window.claude.complete; provider/model as configured by host"), discloses enabled context leaves the browser; Accept → consentEvents grant('model-processing'); Decline → scripted-only. Row on Data screen; revocable.
7. A11y: prefer real <button>/<label>/<input>; role-emulation only where impractical. Toggle = button role="switch" aria-checked. Toast role="status" aria-live="polite". Back chevrons aria-label.
8. PRESERVE original `inCommon Prototype.dc.html` AND V1.1 report unchanged (historical artifacts). ALL repairs go in fork `inCommon Prototype V1.2.dc.html`. No scope expansion, no Phase 1 claim.

## Core module API (write next — signatures aligned to handoff/calculation-fixtures.json)
VERSION const. reduce(n,{preserveMasters}) w/ steps trace; lifePath('1992-07-02')→{value:3,work}; personalYear(md,'2026')→1; personalMonth(py,7)→8; expression(name)→6; soulUrge(name)→7 (Y-as-consonant default, policy param); personality(name)→8; birthday(day)→2; letterMap Pythagorean; angelAnalyze(raw)→{clean,digits,steps,root,repeating,master,disp} (mirror prototype logic exactly; '29'→11 master, '1212'→root 6, '777'→root 3+repeating); fmtToday(date)→'Saturday · July 19, 2026'; fmtShort(date)→'Jul 19'; deriveVisibility(timeMode)→rows {rising,moonDegree,hd,sabian,planetsBySign,numerology} each 'exact|window|suppressed|ok'; createPersistence(storage,{keys,debounceMs,now})→{schedule(getState),cancel(),deleteAll()→{removed,verifiedAbsent},isDeleting()} guard blocks schedule after deleteAll; consentReducer(events,{scope,action,via,ts}); deriveLedger(events); SOURCE_REGISTRY + validateCitations(text,{scopes,records})→{ok,chips,errors,text}; msgScopes(cites)→scope[] (store on each stella msg for history filtering); filterHistory(msgs,scopes); sanitizeForPrompt(text)→«wrapped», stripped, capped 200, SOURCES: removed; crisisCheck(text) input+output (regex from prototype); withTimeout(promise,ms).
Tests consume calculation-fixtures.json directly (fetch in browser, require-ish read in Node via fs) + arbitrary-input vectors (e.g. lifePath('2000-11-29')→11 master path, expression('John Smith'), angel '0', '11:11', empty, non-digits) + the two audit Stella repros (start-branch leak w/ chart off; missing-SOURCES acceptance) as regression tests against core validators + scripted-branch scope fn.

## Remaining fork edit list (V1.2 prototype) — worklog step 3 details still valid below (B1–B6, H1/H2/H5, dates, labels, banners); ADD: profile source of truth replacing ob demo (PERSIST_KEYS += profile, consentEvents, providerConsent; ob completion commits profile + consent events; Today/Stella greet use profile.name; numerology screen computes via core from profile; timeMode suppression per deriveVisibility); storage-ns ?test=1; __incommon hook; helmet script load of core.


Controlling doc: uploads/inCommon_Independent_Source_Audit_V1.0.md (16-item repair list, findings B1–B7, H1–H6, plus "Additional correctness issues").

## Deliverables required
1. Verification Report V1.2.dc.html (V1.1 retained unchanged)
2. Amended source bundle (handoff/)
3. Executable automated tests (Test Runner DC + handoff/incommon-core.js pure-function module)
4. Raw test results (handoff/test-results-v1.2.json — generate by executing tests via run_script)
5. Change log mapping every finding → code + evidence (in V1.2 report + handoff/changelog-v1.2.md)
6. Unresolved findings list (expect: B7 production build = UNRESOLVED/deferred; real ephemeris/lunar/timezone engines = UNRESOLVED → demo-labeled; H6 modality matrix rows mostly SPEC'D/DEFERRED; two-person consent = SIMULATED label)

## Prototype read progress (1998 lines total; read 1–560 so far)
- L33: Today header "Friday · July 18, 2026" hard-coded (wrong weekday) + "High confidence" badge → compute date live, demote confidence label
- L139: Natal "High" badge; L155: "Engine v0.3.1 · reference-tested · historical timezone resolved" → remove/mark demo
- L217: Stella header "Guide · 30 years of practice · AI — cites your charts" → replace per H5
- L204: natal footer "validated ephemeris engine (license under review)" claim → demo wording
- Data & Consent screen ~L470–540: ledger {{ ledger }}, delArm/delConfirm/delCancel, "What we hold" hard-coded counts
- Screens seen so far: Today, Library, Natal (Beginner/Advanced), Stella, Profile, Method, Insights, BaZi, Sabian, Vedic, Data, Moon (starts ~L544)
- Still to read: L560–1998 (Moon, Time Machine, Numerology, Angel, Tarot/I Ching, Compare, onboarding, assessments, Throughline TL array, logic class: PERSIST_KEYS, persistState debounce L1413–1433, delConfirm L1816–1823, ob completion L1754, moonPhases, coreNums, Stella scripted branches + crisis gate + window.claude.complete, revoke(), Maya profile)

## Plan (decided — execute, don't re-deliberate)
1. Read rest of prototype (560→1998), skim Verification Report V1.1.dc.html + uploads/inCommon_Fable_5_Master_Build_Prompt.md (for H6 modality list) + handoff docs
2. Write handoff/incommon-core.js (ES module, pure fns): angel reducer (moved from app), numerology (lifePath/expression/soulUrge/personality/birthday/personalYear from DOB — implement live per audit item 5), createPersistence(storage,{debounceMs}) with cancel+deleteAll guard (B1), consent event reducer (grant/revoke → ledger events + data removal flag) (B5), Stella scope filter + citation validator vs enabled scopes + missing-SOURCES failure + insight-record sanitizer (B6 partial), crisis gate fn, weekday/date formatter. App imports it via dynamic import() in componentDidMount or x-import.
3. Prototype edits: B1 deletion guard using core module; B2 label onboarding "non-persistent preview" banner + reclassify; B3/B4 global DEMO MODE banner component on affected screens (Natal, Today, HD, Compare, Stella context note, Time Machine, Moon, Numerology values now computed live from Maya DOB via core), remove engine-version claims; B5 revoke() does real state mutation (remove Jordan compare data, append ledger event) + label two-person flows SIMULATED; B6 scripted-branch scope fixes, SOURCES validation, history filtering on revoke, provider-consent modal before live Stella, timeout, injection sanitization; H1 "illustrative prototype" labels + honest durations; H2 merge insRecords into TL rendering; H5 copy swap; fix dates → real timestamps (Date.now() on save; today computed); I Ching label "demo subset — 5 of 64"; notify toasts → honest "prototype: no delivery" wording or persist notif registration flag; a11y pass via run_script (role="button" tabindex="0" onKeyDown on onClick divs + focus-visible outline reset in helmet, aria-labels on nav/inputs)
4. Tests: handoff/tests.js (assert helpers, ~30 tests incl. B1 regression w/ mock storage + fake timers, numerology vs calculation-fixtures.json, scope leaks incl. the two audit repros, citation validation, crisis gate, consent reducer, angel fixtures, date weekday). Test Runner.dc.html runs them, renders pass/fail + raw JSON. Generate handoff/test-results-v1.2.json via run_script executing same module code (strip export via new Function OK in sandbox).
5. Verification Report V1.2: copy V1.1 structure; reclassify per audit (numerology→PASS only if now computed+tested, else DEMO; tz/moon→DEMO; onboarding→DEMO/preview; consent→SIMULATED/PARTIAL; Stella→PARTIAL; assessments→DEMO illustrative; Throughline→PARTIAL if wired; themes→PARTIAL; a11y→PARTIAL) + full modality matrix (PASS/DEMO/GATED/SPEC'D/DEFERRED per master prompt list) + change-log table (finding → file:line → evidence/test id) + unresolved list. No % claim vs 80% gate unless matrix complete; do NOT claim Phase 1.
6. Amend handoff docs: README (honest network deps: unpkg React/Babel, Google Fonts, optional live Stella egress; no "local copy" claim), local-storage-schema (notifs = array; add deletion-guard + consent-events + provider-consent keys), stella-grounding (new contract: scope enforcement, SOURCES required, history filtering, injection defense, output check, timeout), changelog-v1.2.md
7. ready_for_verification on Verification Report V1.2.dc.html (user currently viewing prototype — surface report; link to prototype + Test Runner)

## Read-state (updated)
- FULL prototype read (all 1998 lines) — key line refs confirmed: persistState debounce ~L1413, delConfirm removes keys then setState→persist re-writes (B1 confirmed), ob completion toast 'profile unchanged', PERSIST_KEYS excludes ob, sysPrompt has '30 years' + engine v0.2 claims + PY1/transits leak via canned('start') branch cites Numerology even when chart off (cites depend on c.chart but text always discloses PY1 — B6.1), reply() accepts missing SOURCES (empty cites), saveStellaMsg/saveInsight hard-code 'Jul 19'/'Jul 18' dates, tarot/iching/angel saves hard-code dates, revoke() = toast only, ledger hard-coded, exportData includes persona birth, I Ching = 5 hexagrams, Enneagram ENNQ keys only 1,2,4,5,7,9 (3,6,8 impossible), B5 one q per trait, notifs array [8 bools], themes = THEMES tokens, wheel/bodygraph createElement OK.
- V1.1 report FULLY read: structure = doc-page letter; sections: header block, tallies, chg list, §1 matrix (matA 14 launch-critical, matB 7 post-foundation, matC 5 licensing-gated), §2 calcs (12 rows), §3 ai T1-T11, §4 phases P0-P6 + gaps + decisions + change-request box, §5 engines (6 cards), §6 bundle files + deps + standing hold, sign-off. CHIP map keys: PASS, DEMO, PARTIAL, GATED, OPEN, PROTO PASS, SPEC'D. V1.2 must ADD statuses: FAIL, SIMULATED, and per-audit reclasses; new § for change log finding→fix→evidence + unresolved; new modality matrix rows (H6); test-results section pointing at Test Runner + raw JSON.
- Reclassifications required by audit vs V1.1 rows: onboarding PASS→DEMO (B2); confidence layer PASS→DEMO/FAIL (B4 — demo data labeled High); numerology PASS→ depends: implement live functions in core module + tests → keep PASS w/ evidence, else DEMO (choose: IMPLEMENT live via core module, values computed from Maya profile — audit item 5 allows either; implementing is better and small: lifePath/expression/soulUrge/personality/birthday/personalYear already have fixtures); tz resolution PASS→DEMO (static text); moon phase PASS→DEMO (static array); assessments PASS→DEMO (H1); Insight+Throughline PASS→PARTIAL/DEMO (H2 — wire insRecords into TL to make PARTIAL honest); themes PASS→PARTIAL (H3 four-worlds req); T1 PASS→FAIL/PARTIAL (source chips don't prove grounding; missing-SOURCES accepted); T2 PASS→PARTIAL (scripted start leaks PY1 w/ chart off — B6.1); T8 PASS→PARTIAL (insRecords raw interpolation = injection path B6.5); T4 keep PASS but verify purge propagates (forgetChat leaves _greet; persisted store updated — ok); T10 recheck; deps section: 'Executable code SELF-CONTAINED' → FALSE per B7 (unpkg React/Babel via support.js) — rewrite; 'no network writes' claim → correct for live Stella egress (B6).
- Still unread: master prompt (need H6 modality list + § numbers) — grep for module names; handoff/*.md current text (amend pass).

## H6 modality matrix rows (from master prompt — confirmed)
§4A birth-calculated: Western astrology · Pythagorean numerology + Life Path/Personal Years · Chaldean numerology · Sabian symbols (360°) · Chinese zodiac/BaZi · Human Design (§7) · Vedic astrology · Gene Keys (license-gated shell + official link) · Mayan/Tzolkin calendar · Elven Star/sevenfold Sabian (legal-review-gated).
§4B observation-based: Angel numbers · Synchronicity log · Moon phases & lunar planning · Dream journal + non-diagnostic reflection · Tarot journal · I Ching consultation journal · Chakra self-reflection · Archetype exploration · Meditation practices · Rituals & affirmations.
§4C assessments: 16-type/Jungian (legally safe) · Enneagram · Big Five · Attachment styles · Love languages/original relational-preference · Values · Strengths · Spiritual gifts.
Statuses per audit: PASS / DEMO / GATED / SPEC'D / DEFERRED. In prototype today: astrology DEMO, Pythagorean num → PASS-with-tests (implementing), angel PASS, moon DEMO, HD DEMO, BaZi/Sabian/Vedic GATED, Gene Keys GATED, tarot PASS (proto, real dates after fix), I Ching DEMO (5 of 64 — label), Enneagram/Big5 DEMO illustrative, Chaldean SPEC'D (mentioned in Method footer), synchronicity log PARTIAL (angel ctx logging approximates it) → honest: DEFERRED, Tzolkin/dream/chakra/archetype/meditation/rituals/affirmations/16-type/attachment/love-languages/values/strengths/spiritual-gifts DEFERRED (not in build), Elven Star DEFERRED/GATED.
§ numbers confirmed: §3 onboarding, §4 library, §6 astrology, §7 HD+other traditions, §8 Compare, §9 profile, §10 Today+Stella, §11 insight records, §12 persistence/notifs, §13 themes, §14 content, §15 privacy, §17 admin, §18 ownership, §20 no-production-readiness clause, §21 phases, §23 matrix grouping.

## Constraints from user (m0002)
- No features, no responsive pass, no deck, no Phase 1 acceptance claim
- Nothing marked PASS from displayed values / fixture arithmetic / toasts / unexecuted test descriptions — PASS requires an executed test or live computation evidence
- V1.1 unchanged
