# GENERATED by tools/build-bundle.js. Do not edit by hand.
#
# Serves the app when deploy/ is the deploy. It is a shelf rather than a site, so without this the folder called deploy is the one thing that looks right to upload and is not.
#
# Regenerated on every build, so it always names the current bundle.

# The bare address is the cover. Everything else is the app.
/                                  /v6.3/index.html   200
/index.html                        /v6.3/index.html   200
/app.html                          /v6.3/app.html   200
/favicon.svg                       /v6.3/favicon.svg   200
/icon-192.png                      /v6.3/icon-192.png   200
/icon-512.png                      /v6.3/icon-512.png   200
/manifest.json                     /v6.3/manifest.json   200
/minor-bodies-ephemeris.js         /v6.3/minor-bodies-ephemeris.js   200
/react-18.3.1.production.min.js    /v6.3/react-18.3.1.production.min.js   200
/react-dom-18.3.1.production.min.js /v6.3/react-dom-18.3.1.production.min.js   200
/robots.txt                        /v6.3/robots.txt   200
/splash.png                        /v6.3/splash.png   200
/sw.js                             /v6.3/sw.js   200
/three.core.js                     /v6.3/three.core.js   200
/three.module.js                   /v6.3/three.module.js   200

# Every other address is a hash route, so it resolves to the app rather
# than to the cover: an address that names a screen goes to the screen.
# The cover handles the one case the server cannot see, a hash on the
# bare address, by forwarding it in the page.
/*                                 /v6.3/app.html   200
